rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all to diagnose if rules are even being applied
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
